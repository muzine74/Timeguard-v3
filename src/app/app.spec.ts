// Point d'entrée des tests unitaires Jasmine/Karma.
// Ce fichier satisfait tsconfig.spec.json (include: src/**/*.spec.ts).
// Les tests de service sont dans leurs fichiers *.service.spec.ts respectifs.

describe('TimeGuard — Bootstrap', () => {
  it('devrait passer (smoke test)', () => {
    expect(true).toBe(true);
  });
});
